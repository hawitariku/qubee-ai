# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.9        # >=0.0.7 fixes CVE-2024-24762
aiofiles==23.2.1               # required by FastAPI StaticFiles for async file serving

# Templating
jinja2==3.1.2

# ML & NLP (only active when USE_ML=true; torch requires Python <=3.11)
transformers==4.35.2
torch==2.1.1
sentencepiece==0.1.99

# Web Scraping & Corpus Expansion
beautifulsoup4==4.12.2
lxml==5.1.0                    # 4.9.x has security advisories; 5.x is the maintained branch
requests==2.31.0

# PDF Processing
pypdf==4.1.0                   # PyPDF2 is unmaintained; pypdf is the maintained successor

# Utilities
python-dotenv==1.0.0

# Redis (rate limiting — gracefully falls back to in-memory if REDIS_URL is unset)
redis==5.0.1
